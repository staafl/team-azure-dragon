@(Html.Kendo().DropDownList()
    .Name("Exercise") // Name of the widget should be the same as the name of the property
    .DataValueField("ExerciseId") // The value of the dropdown is taken from the EmployeeID property
    .DataTextField("Name") // The text of the items is taken from the EmployeeName property
    .DataSource(datasource => datasource.Read(read => read.Action("GetExercises", "Question")))
    //.BindTo((System.Collections.IEnumerable)ViewData["exercises"]) // A list of all employees which is populated in the controller
    .OptionLabel(" ")// "Please select:"
    .HtmlAttributes(new { style="min-width:100px, max-width:200px"})
)
